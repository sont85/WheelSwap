<!-- <<<<<<< HEAD
<div class="container">
  <h2>Marketplace</h2>
  <div ng-repeat="car in carInventory" class='mylistings'>
    <img ng-src='{{car.imageUrl}}' height='225px'>
    <h2>Make/Model: {{car.model}}</h2>
    <ul>
      <li>Year: {{car.year}}</li>
      <li>Condition: {{car.condition}}</li>
      <li>Color: {{car.color}}</li>
      <li>Seller: {{car.userName}}</li>
    </ul>
    <a ui-sref="trade" ng-click="selectedTradeCar(car)">Make A Trade</a>
======= -->
<div class='container'>
  <div class='marketplaceheader'>
    <h2 class="marketplace">Marketplace</h2>
    <h4 class='instructions col-md-8 col-md-offset-2'>Browse through the cars that have been put up for trade.
      If you come across one you would like, click the 'Make A Trade'
      button and make an offer from the cars in your inventory
    </h4>
  </div>

  <div ng-repeat="car in carInventory" class='thelistings'>
    <div class='col-md-3 col-md-offset box'>
      <img src='{{car.imageUrl}}' class='marketplaceimg'>
      <div class='buttonholder'>
        <ul class='listinglist'>
          <li>Make/Model: {{car.model}}</li>
          <li>Year: {{car.year}}</li>
          <li>Condition: {{car.condition}}</li>
          <li>Color: {{car.color}}</li>
          <li>Seller: {{car.userName}}</li>
        </ul>
        <button class='btn btn-default tradebutton'>
          <a ui-sref="trade" ng-click="selectedTradeCar(car)">Make A Trade</a>
        </button>
      </div>
    </div>
  </div>
</div>
